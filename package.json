{
  "name": "album-deck",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check web/app.js && node --check api/config.js && node --check api/healthz.js && node --check scripts/build-vercel.mjs && node --check scripts/build-windows-zip.mjs",
    "build:vercel": "node scripts/build-vercel.mjs",
    "build:windows-zip": "node scripts/build-windows-zip.mjs"
  }
}
